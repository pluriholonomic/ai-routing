# captured data lives in the HF dataset repo, never in git
data/
discovered_endpoints.json

# local credentials; never commit or publish
.secrets/

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/

"""Empirical screen: is the inference-routing market structurally DeFi-like?

One module per hypothesis (h1..h6), each exposing run(out_dir) -> dict of
results, writing tidy parquet + figures. See plan: hypotheses are pre-specified;
phase-2 tests (h7-h9) are pre-registered in the memo and unlock as the panel
accumulates.
"""

% NeurIPS submission draft.  Replace the geometry preamble with the official
% NeurIPS style at submission time; the content and page audit are style-neutral.
\documentclass[10pt]{article}
\usepackage[margin=0.82in]{geometry}
\usepackage[T1]{fontenc}
\usepackage{times}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{natbib}
\usepackage{multicol}
\usepackage[hidelinks]{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}{Proposition}
\newtheorem{definition}{Definition}
\newcommand{\E}{\mathbb E}
\newcommand{\1}{\mathbf 1}
\newcommand{\OR}{\textsc{OpenRouter}}
\setlist{nosep,leftmargin=*}
\setlength{\emergencystretch}{2em}

\title{Price and Score in Strategic Routing Markets:\\
A Property-Tested Multi-Agent Environment}
\author{Tarun Chitra\\Gauntlet\\\texttt{tarun@gauntlet.xyz}}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Inference routers allocate token demand among strategic providers using public
prices and hidden service scores.  We introduce a request-level multi-agent
environment in which providers choose quotes, admission, and availability
against pluggable routers, with exact settlement, capacity, fallback, common
random numbers, and provider-private feedback.  A property ladder separates
software invariants, held-out moments, simulator causality, strategic robustness,
and live transport.  The first calibration task decomposes price and score:
22,330 public-menu snapshots imply 1.40--10.98 percentage points of mechanical
share from benchmark undercutting, while a prospective owned-choice panel for
the latent score remains support-gated.  Separately, two synthetic learner
benchmarks test failure modes without explaining the public share surface.
Marginal-preserving signal coupling raises buyer price by
0.118 in a focal UCB/UCB game but by $-0.00047$ across heterogeneous learners.
A hardened score reduces one-shot quote and identity manipulation, yet leaves
9.68 times more normalized post-UCB exploitability in its first learning screen.
The result is an executable standard for testing price--score mechanisms, not a
claim that simulated agents reproduce live providers.
\end{abstract}

\section{Introduction}

AI inference is becoming a strategic allocation problem.  A harness sends a
request naming an open-weight model; a router chooses among providers that run
that model; and providers choose price, admission, and service quality.  The
objects are partly substitutable but not identical.  Sampling, quantization,
tool-call support, latency, privacy, and hidden capacity alter the delivered
product even under a common model label.

This is a natural multi-agent learning environment.  Providers repeatedly act,
receive private profit feedback, and adapt against a router that transforms
their actions into demand.  It is also a dangerous environment to study
naively.  A public price menu is not a firm execution commitment.  A documented
inverse-price score is not necessarily realized selection after eligibility and
fallback.  A simulator can generate clean welfare and collusion statistics even
when the corresponding live quantities are unobserved.

We propose a different standard: a routing simulator should be
\emph{property-tested}.  Its internal invariants must hold exactly; its
observations must match the information available to real agents; its
calibration targets must survive holdout; causal simulator interventions must
preserve declared nuisance structure; and a separate transport gate must pass
before interpreting a simulated mechanism in the live market.  Failed gates are
results rather than inconveniences.

We implement this standard in \texttt{orcap}, a deterministic request-level
kernel with a lightweight parallel multi-agent API.  A provider action contains
a quote, admitted-capacity fraction, and availability state.  A router plugin
returns an attempted-provider order.  Settlement applies capacity constraints,
technical failure, fallback latency, payment, variable cost, capital cost, user
utility, and welfare request by request.  Stable hash-derived substreams give
common random numbers across mechanism arms.  Provider observations expose
public quotes and only the provider's own realized settlement; rival cost,
capacity, and reliability remain private.

The environment includes random, inverse-price, quality-adjusted, synthetic
price-cut-memory, menu-projected, and hardened adaptive routers.  Provider policies include
static regimes, calibrated species, tabular Q-learning, UCB, epsilon-greedy, and
an optional LLM adapter.  Global finite-grid best responses, two-provider
coalitions, identity splitting, fading quotes, and sequential response oracles
audit strategic robustness.  The memory router is an adversarial benchmark with
a declared transition law; it is not calibrated from the public price panel.

Two tasks are intentionally orthogonal.  E0 measures a contemporaneous
price--score manipulation surface.  E1 asks whether particular learners fail
under correlated signals and reward memory inside a synthetic game.  Neither
task supplies the missing transition equation for the other.  They can be joined
only after owned routing outcomes show that lagged price or quality histories
predict a future score beyond the current menu and after a causal design
separates memory from unobserved eligibility.

Our evaluation has four main findings.

\begin{enumerate}
\item Public menus identify a price-manipulation surface but not realized flow.
Across seven models, resetting active quotes to model-author benchmarks removes
1.40--10.98 percentage points of conditional shadow share.  A prospective paid
panel estimates the non-price score and its interaction with undercutting; it is
still accruing, so no score result is imputed.
\item In a separate synthetic benchmark, an exact signal-order intervention validates a narrow
Hansen--Misra--Pai-style mechanism \citep{hansenmisrapai2021}.  In a focal
two-UCB prisoner's-dilemma game, preserving common signal order increases
exploration coupling, joint high-price play, and buyer price.  The two-seed
factorial has 1,440 rows and 720 paired cells; the buyer-price effect is $+0.118$.
The effect disappears under UCB/epsilon/static mixtures ($-0.00047$).  The
executable verdict is therefore negative on transport.
\item Hardening an adaptive score with lagged inputs, commitment epochs,
operator-level caps, and one-sided share trust regions works mechanically.
Across 4,878 historical menus it reduces mean maximum quote-attack gain from
0.226 to 0.030 and removes the measured identity-splitting gain.  Yet the first
UCB screen ends at profiles with normalized residual exploitability 3.929 versus
0.406 under inverse-square routing.  Protecting a rule from one-step attacks can
make its learning dynamics harder.
\item Public and owned data sharply limit external interpretation.  A residual-
coupling statistic reaches an unadjusted $p=0.04998$, but one provider pair
accounts for 87.3\% of paired events, two later mechanism legs are power-gated,
and the multiplicity family is incomplete.  We do not identify collusion,
communication, or provider algorithm.
\end{enumerate}

The contribution is methodological and executable.  Existing multi-agent
frameworks provide general APIs \citep{terry2021pettingzoo,lanctot2019openspiel};
algorithmic-pricing work demonstrates emergent coordination in stylized demand
systems \citep{calvano2020}.  We add request-level inference-market accounting,
mechanism plugins, empirical property gates, and negative transport results.

\section{The Strategic Routing Environment}
\label{sec:environment}

\subsection{Timing and primitives}

There are providers $i\in\{1,\ldots,n\}$.  Provider $i$ has private technology
\[
\xi_i=(c_i,K_i,F_i,\rho_i,L_i),
\]
where $c_i$ is attempt cost, $K_i$ physical capacity, $F_i$ capital cost per
slot, $\rho_i$ reliability, and $L_i$ base latency.  At epoch $t$, it chooses
\[
a_{it}=(p_{it},\kappa_{it},z_{it}),
\]
where $p_{it}>0$ is its quote, $\kappa_{it}\in[0,1]$ its admitted-capacity
fraction, and $z_{it}$ is active, degraded, or withdrawn.  Effective capacity is
the integer floor of $K_i\kappa_{it}$ times a degradation multiplier.

A workload specifies input and output tokens, delivered value $v$, failure loss
$\ell_F$, latency cost $\ell_L$, fallback latency, and a maximum number of
attempts.  A router observes public actions and whatever attributes its plugin is
allowed to use, then returns an ordered list of eligible providers for each
request.  Each admitted attempt consumes capacity and cost even if it fails.
Payment goes to the first successful provider.  Capital cost is paid each epoch,
including when capacity is withdrawn.

\subsection{Exact settlement}

For request $r$, let $j(r)$ be the serving provider, if any; $A(r)$ the attempted
providers; $P_r$ its payment; $C_r=\sum_{i\in A(r)}c_i$ resource cost; and $T_r$
total latency.  User utility and pre-capital welfare are
\begin{align}
u_r&=\begin{cases}v-P_r-\ell_LT_r,&j(r)\ne\varnothing,\\
-\ell_F-\ell_LT_r,&j(r)=\varnothing,
\end{cases}\\
w_r&=\begin{cases}v-C_r-\ell_LT_r,&j(r)\ne\varnothing,\\
-\ell_F-C_r-\ell_LT_r,&j(r)=\varnothing.
\end{cases}
\end{align}
Provider profit is revenue minus attempt and capital costs.

\begin{theorem}[Settlement reconciliation]
\label{thm:settlement}
For every seed, action profile, demand realization, capacity profile, router
plugin, and fallback path generated by the kernel,
\begin{equation}
W_t=U_t+\sum_i\pi_{it}.
\end{equation}
No reliability, independence, or nonbinding-capacity assumption is required.
\end{theorem}

The identity is enforced in tests, as are capacity conservation, nonnegative
payments, attempted-provider ordering, and deterministic replay.  If a router fee
or SLA transfer is added, it enters both sides explicitly.

\subsection{Parallel RL interface and information boundary}

The environment exposes
\begin{center}
\texttt{reset(seed)} $\to \{i:o_{i0}\}$,\qquad
\texttt{step(actions,demand)} $\to (o',r,done,info,result)$.
\end{center}
The observation $o_{it}$ contains epoch, demand, public quotes, and $i$'s own
profit, attempts, successes, technical failures, and capacity rejections.  It
does not contain rival technology $\xi_{-i}$.  The complete market result remains
available to an evaluator.  This separation prevents a common simulation error:
training agents on planner-only data and then interpreting their behavior as a
market equilibrium.

The API is dependency-light and can be adapted to PettingZoo's parallel
interface.  It deliberately treats one epoch as a simultaneous provider move.
Within-epoch routing and fallback occur after actions are committed; router state
advances exactly once per epoch.

\subsection{Mechanism and strategy plugins}

The baseline inverse-price router uses
$x_i\propto p_i^{-\eta}$.  This is a counterfactual mechanism, not an empirical
claim about \OR.  Adaptive routers may additionally use reliability or quality.
The common interface writes
\begin{equation}
x_i(p,\alpha)=
\frac{p_i^{-\eta}e^{\alpha_i}}{\sum_jp_j^{-\eta}e^{\alpha_j}},
\qquad
p_i^{\rm eff}=p_i e^{-\alpha_i/\eta}.
\label{eq:price-score}
\end{equation}
Thus every non-price score is a price-equivalent subsidy or penalty.  The
environment can reveal $\alpha_i$ to the provider, reveal only its realized
attempts, or delay score feedback; the information policy is part of the router
plugin and scenario hash.  If $\alpha_i=h_i(\log p_i)$, negative own-price
elasticity is $(1-x_i)(\eta-h_i')$, making score attenuation or amplification
of undercutting directly testable.
The hardened rule includes five defenses: lagged smoothed inputs, provider-
specific leave-one-out parameters, multi-epoch commitments, operator-neutral
exploration and share caps, and a one-sided log-share trust region.  Allocation
gains are capped; providers are not insured against losing share after raising
price or quality risk.

Strategies share the same action interface.  Static policies reproduce fixed
menus; species policies reproduce descriptive price regimes; Q and UCB policies
learn from own reward; and scripted adversaries search quote, capacity, outage,
coalition, and identity-splitting actions.  A strategy name is never treated as a
claim about a live provider.

\section{Property Testing and Transport}
\label{sec:properties}

\begin{definition}[Property ladder]
A simulator claim at level $k$ may use evidence from levels $1,\ldots,k$ but may
not inherit claims from a higher level:
\begin{enumerate}
\item accounting and transition invariants;
\item information-set and intervention fidelity;
\item held-out empirical moments;
\item adversarial and heterogeneous-strategy robustness;
\item prospective transport to owned or partner routing outcomes.
\end{enumerate}
\end{definition}

Level 1 is exact software validation.  Level 2 asks whether an intervention
changes only its declared object.  Level 3 asks whether the environment preserves
properties visible in held-out data.  Level 4 tests whether a result survives
agents unlike the focal learner and profitable deviations outside its path.
Level 5 is required for a live mechanism or welfare claim.

\subsection{Current market properties}

The latest hosted aggregate revision spans 7--21 July 2026.  In its frozen
provider-regime analysis, 366 training provider-model pairs produce 358 evaluable
holdout pairs.  Overall regime persistence is 0.944.  Anchor, premium, and static
discount regimes persist at 1.000, 0.992, and 0.976; active undercutting persists
in 5 of 21 pairs (0.238).  The regimes describe quote paths, not cost or intent.

An active-undercutter response screen contains 1,052 paired timing events in 11
provider-model clusters.  The registered response-minus-shifted-placebo
difference is 0.0019 with cluster interval $[-0.0438,0.0410]$ and one-sided exact
$p=0.468$.  The current owned delegated-selection match contains 81 anchor and
92 premium selections, with no active or static selections.  This is support for
our workload, not market share.  Zero provider-model cells have a valid capacity-
utilization measure, and dumping is not identified.

An independent elasticity audit rejects the claim that the public inverse-square
formula is realized allocation ($z=10.06$).  Consequently, the environment does
not use public shadow shares as a live calibration target.  It uses them only to
define explicit counterfactual router plugins.

The price--score decomposition makes that distinction constructive.  At a
fixed 1,000-input/256-output-token shape and point exponent $1.6483$, 22,330
snapshots across seven models produce benchmark-reset active-share effects from
1.40 to 10.98 percentage points.  GLM-5.2's 27.3\% median equivalent discount
produces 10.98 excess shadow-share points and 5.40 points of anchor loss.  The
full menu, not the cut alone, determines the effect.

The level-5 companion is an assignment-first GLM-5.2 campaign.  Conditional
multinomial choice estimates the relative $\alpha_i$ in
\cref{eq:price-score}, its price-equivalent discount, mean probability mass
reallocated beyond price, and whole-block out-of-sample information.  It also
compares explicit price sorting with default routing.  The prospective cutoff
is 21 July 22:00 UTC; four earlier observed choices are excluded.  Diagnostic
support requires 40 choices, 20 blocks, and three selected providers;
publication requires 800 choices, 100 blocks, seven days, and 90\% menu
coverage.  At this freeze it has zero eligible choices.  A simulator may use the
public surface now, but may not claim live score calibration until that gate.

\begin{figure*}[t]
\centering
\includegraphics[width=0.96\textwidth]{../../data/analysis/undercutting-incidence-v1/wf19_cross_model_elasticity.pdf}
\caption{Level-3 price calibration.  Benchmark resets identify deterministic
share transfers under a declared inverse-power router, not realized flow.  The
prospective owned-choice panel is the separate level-5 bridge for the latent
score and remains support-gated.}
\label{fig:price-score}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.96\textwidth]{../../data/analysis/undercutting-incidence-v1/wf19_model_comparison_timeseries.pdf}
\caption{Level-3 temporal calibration.  Shared axes compare hourly benchmark
discounts, deterministic excess undercutter shadow share, and price-rule arc
elasticity across seven models.  The environment can replay these public menu
regimes, but level-5 scoring and realized-flow claims remain separately gated.
Free endpoints are excluded because inverse-power shares are undefined at a
zero price.}
\label{fig:price-score-time}
\end{figure*}

\subsection{Intervention fidelity}

The signal experiment starts from an $T\times n$ signal matrix $Z$.  Its treatment
arm preserves the original order; its control independently permutes each
provider column.

\begin{proposition}[Exact marginal preservation]
\label{prop:shuffle}
For every provider $i$ and every measurable function $h$,
\begin{equation}
\sum_{t=1}^T h(Z_{ti})
=\sum_{t=1}^T h(\widetilde Z_{ti}).
\end{equation}
Thus the intervention preserves each finite-sample marginal multiset exactly and
changes only cross-provider and temporal ordering.
\end{proposition}

Paired seeds reuse action-policy RNG and structural primitives.  The intervention
therefore identifies a signal-order effect inside the simulator.  It does not
identify the signals, beliefs, or algorithm of a real provider.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{environment-audit.pdf}
\caption{Property tests expose conditional success.  Panel A: exact
marginal-preserving signal intervention; focal UCB/UCB effects vanish across
heterogeneous strategies.  Panel B: the hardened adaptive router reduces
mechanical one-step attacks on historical menus.  Panel C: finite-grid static
audits pass, but normalized post-UCB exploitability is higher.  Panels B--C are
simulator or menu-replay quantities, not live provider profit.}
\label{fig:audit}
\end{figure*}

\section{Experiments}
\label{sec:experiments}

\subsection{E0: price manipulation times latent scoring}

For every provider below a model-author benchmark, E0 replaces only its quote
with that benchmark.  Let $\Delta_i(0)$ be the resulting price-only share gain
and $\Delta_i(\alpha)$ the gain under \cref{eq:price-score}.  The interaction
\begin{equation}
\Gamma_i=\Delta_i(\alpha)-\Delta_i(0)
\end{equation}
is negative when scoring attenuates undercutting and positive when it amplifies
it.  The public panel identifies $\Delta_i(0)$ mechanically.  The prospective
owned-choice panel estimates $\alpha$; until its gate, $\Gamma_i$ is missing,
not zero.  Simulator transport requires matching the sign and magnitude of
$\Gamma_i$, not merely reproducing the public quote distribution.

This task also supplies adversarial policies: unilateral benchmark undercutting,
fading a cheap quote after allocation, identity splitting, quality shading, and
capacity withdrawal.  A router passes only if it lowers these gains without
violating held-out completion, fidelity, entry, and learning-regret gates.

\subsection{E1: common signals and heterogeneous learners}

E1 is an adversarial learning benchmark, not a proposed explanation of E0's
cross-model shadow-share time series.  Its memory and signal processes are
declared simulator primitives.  A live-market interpretation requires a
separate transport experiment on lagged routing scores and quality outcomes.

Two providers choose low price 0.65 or high price 1.00 at marginal cost 0.20.
With router exponent 5, the one-period payoffs satisfy the prisoner's-dilemma
ordering
\[
T=0.4032>R=0.4000>P=0.2250>S=0.0832.
\]
The factorial varies signal-to-noise, common correlation, exponentially weighted
reward memory, router exponent, learner mixture, and seed.  UCB/UCB is the focal
mechanism; UCB/epsilon, epsilon/epsilon, and UCB/static are transport screens.
Each coupled cell is paired with its marginal-preserving shuffle.

The successful hosted run contains 1,440 factorial rows and 720 paired cells.  In
the focal positive-correlation, SNR-at-least-two, exponent-five subset, coupled
signals raise exploration-action correlation by 0.214, joint high-price share by
0.339, and buyer price by 0.118.  Buyer price increases in 69.4\% of focal cells.
The primary focal sign screen passes.

The heterogeneous screen fails.  Its corresponding effects are $-0.0030$,
$-0.0015$, and $-0.00047$; buyer price increases in 49.1\% of cells.  Across all
paired cells the mean buyer-price effect is 0.0131 and the positive share is
49.9\%.  We therefore report a causal mechanism example and a failed universality
claim.  A simulator that published only the focal UCB panel would give the wrong
design impression.

\subsection{Failed external transport audit for E1}

This audit does not connect E1 to E0's market-share accounting.  It asks whether
four necessary properties of the synthetic signal mechanism are even visible in
the external aggregate panel.  The ordered family is: (SC1) excess residual quote coupling;
(SC2) preperiod routing signal-to-noise gradient; (SC3) coupled-versus-independent
elasticity wedge; and (SC4) forward transition toward premium states.  The latest
aggregate monitor has 766 completion-price events, seven mechanism days, 19
provider-pair/model clusters, and 70 covered delegated choices.

SC1's observed residual covariance is 0.02023, exceeding the mean of 2,000
clock-preserving circular shifts by 0.02560.  Its one-sided $p=0.04998$ is nominal,
but the maximum provider-pair event share is 0.873 against a frozen cap of 0.20.
SC2 and SC3 are power-gated.  SC4's descriptive premium-transition association
has $p=0.129$.  The Holm family is incomplete and no claim promotes.  This is not
semantic caution: one more day moved SC1 across 0.05 while worsening pair
concentration.  A fixed property gate prevents optional causal language.

\subsection{E2: static defense versus adaptive learning}

The adaptive-router screen uses 4,878 eligible historical menus, 652,680
historical quote-attack rows, 200 strategic menus, 7,200 cost-by-capacity-by-rule
cells, 32 sequential-response cells, 320 UCB rows, and 40 bounded Q-learning runs.
All artifacts share one immutable input revision.

Against inverse-square routing, hardening reduces mean maximum allocation gain
from 0.2262 to 0.0300, share captured by a 25\% fading quote from 0.4638 to 0.2422,
and two-identity combined-share gain from 0.1499 to zero.  Model-day cluster
intervals exclude zero in the favorable direction.  Finite-grid mean unilateral
exploitability falls from 0.6664 to zero; mean two-provider exploitability from
0.4976 to 0.0690.  All eight hardened sequential best-response cells converge
with zero residual gain within the declared grid.

The learning screen reverses the normalized ranking.  Post-UCB residual
exploitability is 3.9291 under hardening and 0.4057 under inverse-square, a 9.68
ratio.  Absolute gains are much closer ($5.89\times10^{-4}$ versus
$3.64\times10^{-4}$), so lower provider profit contributes to the normalized
failure.  The original UCB seed rows were deterministic duplicates; the correct
independent unit is eight menus.  A later stochastic engineering audit restores
distinct seed paths and finds lower hardened absolute deviation gain on seven of
eight menus, but still fails the frozen normalized gate.  Neither result is
confirmatory.

The design lesson is concrete.  A one-sided trust region can prevent a provider
from suddenly gaining traffic by undercutting, yet introduce committed state that
learners estimate slowly.  Static strategy-proofness on a grid does not imply low
learning regret or fast convergence in a nonstationary repeated game.

\section{Mechanism Evaluation Without Welfare Theater}
\label{sec:mechanism}

The kernel computes user utility, provider profit, and social surplus for every
declared scenario.  Those numbers are internally exact and externally conditional
on workload value, provider cost, and capacity.  Current public data identify none
of these primitives well enough for a market-wide welfare estimate.

We therefore recommend a robust design problem.  Let $\theta$ parameterize score
weights, commitments, exploration, caps, and fees; $\Omega$ be the set of cost,
quality, capacity, and learning models consistent with observed properties.  The
evaluator solves
\begin{align}
\min_\theta\sup_{\omega\in\Omega}\;&
R_U(\theta,\omega)+\lambda_1 E_1(\theta,\omega)
+\lambda_2E_2(\theta,\omega)+\lambda_VV(\theta,\omega),
\end{align}
subject to completion, concentration, budget, and delivered-capacity floors.
$R_U$ is user regret; $E_1$ and $E_2$ are unilateral and two-provider
exploitability; $V$ is constraint violation.  Results should be reported as a
Pareto frontier, with cost/value sensitivity, rather than a single welfare point.

We instantiate four score families rather than treating ``better routing'' as a
scalar label.  The user-cost score minimizes payment plus latency and failure;
the welfare score replaces payment by bounded resource cost and adds delivered
value; the router-revenue score maximizes fees net of retries and SLA credits;
and the quality score maximizes blinded task fidelity subject to spend and
completion constraints.  Each arm reports user utility, provider profit, router
revenue, welfare bounds, quality, concentration, and exploitability.  An arm can
dominate on one vector and fail on another.

The outer game also varies provider technology.  Owned or reserved-capacity
providers pay fixed capital cost and have low short-run marginal cost;
spot-dependent providers have flexible entry and scarcity-sensitive cost;
anchor adopters may keep displayed price fixed; and technology-rent providers
may invest in kernels or hardware.  Entry, capacity, and quality investment are
chosen before the repeated pricing game.  A mechanism improvement must survive
best responses and two-provider coalitions for each technology mixture, not just
reallocate a frozen menu.

The current replay candidate uses price exponent 1.25 and exploration 0.10.  On
18,947 held-out hourly menus it lowers a quote-coupling proxy by 49.3\%, raises
displayed price by 2.08\%, and changes public reliability by 0.0059 percentage
points.  Provider behavior is fixed.  The result is sufficient to choose a
prospective randomized arm, not to deploy the rule or claim welfare.

The next experiment randomizes production delegation, price-ordered fallback,
and the frozen candidate score within model--prompt--time blocks.  Primary
outcomes are attempted-request spend, completion, end-to-end latency, and blinded
task fidelity.  Capacity commitments require a separate provider-facing trial.
A scalar welfare statement is promoted only over a preregistered box of user
values and provider costs.

\section{Related Work}

General multi-agent interfaces and game-learning suites prioritize broad domain
coverage \citep{terry2021pettingzoo,lanctot2019openspiel}.  Our contribution is a
market-specific settlement layer and an empirical transport standard.
Algorithmic-pricing experiments show that independent learners can sustain high
prices \citep{calvano2020}.  Hansen, Misra, and Pai identify a distinct channel
through correlated price experiments \citep{hansenmisrapai2021}; our exact
signal-order intervention isolates that channel and shows its learner dependence.

Brown and MacKay study heterogeneous pricing algorithms and repricing frequency
\citep{brownmackay2023}.  Our persistent quote regimes are compatible with that
motivation but do not identify algorithms.  Johnson, Rhodes, and Wildenbeest show
how platform prominence changes algorithmic price competition
\citep{johnsonrhodeswildenbeest2023}; our adaptive-rule experiments add hidden
capacity, fallback, and learning-speed failures.  Model-routing benchmarks choose
among models \citep{chen2023frugalgpt,ong2024routellm,hu2024routerbench}; we hold
the model label fixed and study competition among execution providers.

\section{Limitations, Ethics, and Conclusion}

The public panel is measured in weeks.  Owned probes cover one account and
workload distribution.  Public enforcement is aggregated.  Provider cost,
market-wide demand, user value, private rebates, and deliverable capacity are
unobserved.  UCB, epsilon-greedy, Q-learning, and static policies span only a
small subset of strategic behavior.  Grid exploitability is not unrestricted
dynamic exploitability.  The hardened-rule screen is mixed and not confirmatory.

Paid probes are assignment-first, minimal-token, budget-capped, and remotely
rate-limited.  Public artifacts remove request-level rows.  The research does not
send adversarial load, infer prompt contents, or identify provider employees.
Provider labels are descriptive regimes, not allegations.  We do not claim
collusion, communication, dumping, or literal front-running.

The central finding is that routing-market simulation needs a refusal mechanism
and an explicit price--score boundary.  Public menus calibrate strategic
price-induced transfers; owned choices are required for hidden scoring; neither
licenses cost, welfare, conduct, or score-memory claims by itself.  The focal
UCB result is real inside its separate synthetic game and fails across heterogeneous agents.
The hardened router resists one-step manipulation and fails its first learning
gate.  The public quote signal is nominal and fails concentration and family
completion.  A property ladder makes those boundaries executable.  That is what
turns a strategic simulator from a storytelling device into a scientific
instrument.

\appendix

\section{Proofs}

\paragraph{Proof of \cref{thm:settlement}.}
For a successful request, user utility plus the serving and attempted providers'
variable profit is
$(v-P_r-\ell_LT_r)+(P_r-C_r)=v-C_r-\ell_LT_r=w_r$.
For a failed request it is
$(-\ell_F-\ell_LT_r)-C_r=w_r$.  Sum over requests and subtract each provider's
epoch capital cost on both sides. \hfill$\square$

\paragraph{Proof of \cref{prop:shuffle}.}
For each $i$, $\widetilde Z_{\cdot i}$ is a permutation of $Z_{\cdot i}$.
Summation is invariant to permutation. \hfill$\square$

\section{Reproducibility and Evidence Ledger}

The RL wrapper is \path{src/orcap/market_env/rl_env.py}; its tests verify
private-information boundaries, reward reconciliation, horizons, invalid calls,
and exact reset replay.  A documented four-epoch example at
\path{examples/strategic_routing_demo.py} is itself tested for
byte-equivalent seeded output and settlement reconciliation; the adjacent
environment guide specifies the observation contract and custom-router path.
Kernel tests additionally cover capacity, failure,
fallback, transfers, and stable random substreams.  Signal tests verify exact
column multisets and paired interventions.  Adversarial tests cover quote
monotonicity, share caps, commitment state, identity grouping, unilateral grids,
and coalition enumeration.

The WF18 hosted evidence is GitHub Actions run 29834114219, code revision
\texttt{4e89018}, and dataset revision \texttt{e4174d08}.  The adaptive screen is
run 29780088479, code revision \texttt{b7908e09}, and dataset revision
\texttt{0a7c6bef}.  Aggregate manuscript inputs are recorded in
\texttt{papers/neurips/hosted-evidence.json}; the figure is generated by the
adjacent build script.  The hosted WF18 job hydrates one immutable snapshot,
runs WF16 and WF18, executes the two-seed simulator, validates privacy boundaries,
removes owned request rows, publishes aggregates to Hugging Face, refreshes a
private dashboard, and preserves an immutable artifact.
The cross-model price surface and prospective scoring protocol are frozen in
\texttt{papers/price-score-evidence-2026-07-21.json}.  The score cell is
explicitly accruing and contributes no fitted live parameter to the simulations
in this paper.

\section{Environment Card}

\paragraph{Agents and actions.}
The agents are inference providers.  A simultaneous action is a positive quote,
an admitted-capacity fraction in $[0,1]$, and an availability state.  The core
environment does not let a provider change its private marginal cost, physical
capacity, reliability, or base latency during an episode.  Experiments that model
investment do so in an outer stage and create a new immutable provider
specification.

\paragraph{Observations and rewards.}
Every provider sees the epoch, realized demand, the public quote vector, and its
own preceding profit, attempts, completions, failures, and capacity rejections.
The default reward is own profit after variable and installed-capacity cost.
Alternative rewards must be named by an experiment; the environment never hands
social welfare to a provider by default.  The evaluator sees request outcomes,
user utility, provider profit, resource cost, payment, and conditional welfare.

\paragraph{Transition and termination.}
Within an epoch, the router orders candidates, capacity clips attempts,
reliability draws determine technical failure, and fallback continues to the
declared limit.  The episode ends at a fixed horizon; there is no outcome-based
early stopping.  Calling \texttt{step} after termination is an error.  Resetting
with the same seed exactly replays the same transition under identical actions.

\paragraph{Randomness and pairing.}
A stable hash of base seed, epoch, request, event type, and provider creates
disjoint random substreams.  Mechanism comparisons reuse the same base seed so a
routing change does not silently redraw reliability.  Signal interventions have
a separate, recorded seed and assert column-multiset equality before running.

\paragraph{Known failure modes.}
Expected-allocation training removes routing noise and can create duplicate seed
paths.  Normalized exploitability is unstable near zero provider profit.  A
finite quote grid can miss continuous or history-dependent deviations.  Public
menus cannot calibrate hidden eligibility or market-wide demand.  Static action
robustness does not imply learning convergence.  The released screen reports all
five failures rather than treating them as tuning opportunities.

\paragraph{Minimum reporting standard.}
A routing-market experiment should publish: code and scenario hashes; action and
observation schemas; provider information boundaries; seed and pairing policy;
router state-transition timing; transfer reconciliation; capacity and fallback
semantics; calibration/holdout split; algorithm families; global and coalition
deviation class; absolute and normalized exploitability; failed transport gates;
and a statement of which welfare primitives are assumed rather than measured.

\begin{multicols}{2}
\small
\bibliographystyle{plainnat}
\bibliography{../../paper/references}
\end{multicols}

\end{document}
